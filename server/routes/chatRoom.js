import express from "express";
import chatRoom from '../controllers/chatRoom';


const route = express.Router();

//route.get('/', chatRoom.getRecentConversation)
//route.get('/:roomId', chatRoom.getConversationByRoomId)
//route.post('/initiate', chatRoom.initiate)
//route.post('/:roomId/message', chatRoom.postMessage)

export default route;